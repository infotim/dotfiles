- clean: ['~']

- shell:
    - [mkdir -p ~/.ssh, Create ~/.ssh]

- link:
    ~/.dotfiles: ''
    ~/.bash: bash
    ~/.bashrc: bashrc
    ~/.dircolors: dircolors
    ~/.ssh/config: ssh/config
    ~/.tmux.conf: tmux.conf
    ~/.Xresources: x/resources
    ~/.xsession: x/session
