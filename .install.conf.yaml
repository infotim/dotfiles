- clean: ['~']

- shell:
    - [mkdir -p ~/.ssh, Create ~/.ssh]

- link:
    ~/.dotfiles: ''
    ~/.bash: bash
    ~/.bashrc: bashrc
    ~/.ssh/config: ssh/config
    ~/.tmux.conf: tmux.conf
    ~/.Xresources: x/resources
